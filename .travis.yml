language: php
php:
  - '7.3'
  - '7.4'
addons:
  apt:
    update: true
before_install:
  - echo "$pusher_key"
  - composer validate
install:
  - composer install --no-interaction
before_script:
  - composer require squizlabs/php_codesniffer --dev
script:
  - vendor/bin/phpunit --coverage-text --testdox --verbose
  - vendor/bin/phpcs --ignore=app/Http/Controllers/Controller.php app/Http/Controllers
deploy:
  provider: heroku
  api_key: $heroku_api_key
  app: lumen-pusher
  on:
    branch: backend